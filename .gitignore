# Virtual environment
.venv/

# PDFs (test corpus — keep locally, don't track in git)
*.pdf

# Extracted JSON outputs (build artifacts)
*.json
# But track the viewer's npm manifests
!etl/viewer/package.json
!etl/viewer/package-lock.json

# Layout debug image output directories
layout_output*/

# Python cache
__pycache__/
*.pyc
*.pyo

